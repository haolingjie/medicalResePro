var Sub = function (val,preIndex,sufIndex) {
  if (val.length == 0 || val == undefined) {
    return;
  }
  if (val.length >0 ) {
    return val.substring(preIndex, sufIndex);
  }
}
//exports: 通过该属性，对外共享本模块的私有变量与函数。
module.exports.Sub = Sub;
