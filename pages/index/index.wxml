<!--index.wxml-->
<view class="container">
  <!-- <view class="userinfo">
    <button wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 获取头像昵称</button>
    <block wx:else>
      <image bindtap="bindViewTap" class="userinfo-avatar" src="{{userInfo.avatarUrl}}" mode="cover"></image>
      <text class="userinfo-nickname">{{userInfo.nickName}}</text>
    </block>
  </view> 
  <view class="usermotto">
    <text class="user-motto">{{motto}}</text>
  </view> -->

  <view>
    <navigator url="../index/index" class=''>卡号登陆</navigator>
    <navigator url="../idLogin/idLogin">身份证登陆</navigator>
  </view>
  <form bindsubmit='loginSubmit' bindreset='fromReset'>
    <view>
      <input type='text' name='idCard' value='A10000307' placeholder='请输入卡号' />
      <input type='text' name='password' value='ZrPCGpnt' placeholder='请输入密码' />
      <button form-type="submit">提交</button>
    </view>
  </form>
</view>